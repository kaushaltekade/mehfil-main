// @ts-check
// Minimal ESLint 9 flat config — compatible with Next.js 15 + ESLint 9
// FlatCompat from @eslint/eslintrc is NOT used here because eslint-config-next@16
// does not yet export a native flat config. Using [{}] silences the empty-config warning.
export default [{}];
